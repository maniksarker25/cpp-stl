#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
 
    int n;
    int leftShift = (11 << 2);  // kon value left shift korta chaii and koto gor left shift korta chai
    int rightShif = (11 >> 1);
    cout << leftShift << endl;
    cout << rightShif << endl;

    
    return 0;
}