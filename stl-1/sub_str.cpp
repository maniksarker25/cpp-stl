// substring in string ============


#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    
    string s;
    cin >> s;
    // cout << s<<endl;


    // find sub string from string ---------- define starting pos and end pos
    // cout << s.substr(0,3) << endl;

    // if i not define end pos then it go to the end
    // cout << s.substr(2) << endl;

    


    return 0;
}